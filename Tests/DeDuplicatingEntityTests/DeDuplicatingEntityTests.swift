import XCTest
@testable import DeDuplicatingEntity

final class DeDuplicatingEntityTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DeDuplicatingEntity().text, "Hello, World!")
    }
}
